# name: Databricks Pipeline

# on:
#   push:
#     branches: [main]


# jobs:
#   build:
#     runs-on: ubuntu-latest
#     environment: DATABRICKS
#     steps:
#       - name: Checkout repo
#         uses: actions/checkout@v4
      
#       - name: Install tools
#         run: |
#           pip install databricks-cli dbt-databricks


name: Databricks Pipeline

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    environment: DATABRICKS
    steps:
      # - name: Debug
      #   env:
      #     DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
      #     DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
      #   run: |
      #     echo "Databricks host: $DATABRICKS_HOST"
      #     echo "Databricks token: $DATABRICKS_TOKEN"

      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Install Databricks CLI and DBT
        run: |
          curl -fsSL https://raw.githubusercontent.com/databricks/setup-cli/main/install.sh | sh
          pip install dbt-databricks

      - name: Upload CSV
        env:
          DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
        run: |
          databricks fs cp data/inventory-2026.csv \
            dbfs:/Volumes/warehouse/default/raw/inventory-2026.csv \
            --overwrite