bundle:
  name: my_pipeline

workspace:
  host: ${var.databricks_hot}

variables:
  databricks_host:
    description: Databricks Workspace URL

targets:
  dev:
    mode: development
    default: true

  prod:
    mode: production
    workspace:
      host: ${var.databricks_host}